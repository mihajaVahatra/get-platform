import { LoginScreen } from '@/components/auth/LoginScreen';

/** Page de connexion `/auth/login` : délègue tout le rendu et la logique à {@link LoginScreen}. */
export default function LoginPage() {
  return <LoginScreen />;
}
