node_modules
/dist
*.tsbuildinfo
# Keep environment variables out of version control
.env

/generated/prisma
/coverage

# User-uploaded media (runtime content, not source)
/uploads

# Boîte de réception locale des emails simulés (aucun provider configuré) —
# voir NotificationService.writeLocalMailSink. Contient potentiellement des
# liens/codes secrets de test, ne doit jamais être versionné.
/.local-mail
